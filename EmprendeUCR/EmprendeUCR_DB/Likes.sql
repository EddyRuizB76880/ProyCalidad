CREATE TABLE [dbo].[Likes]
(
	[Client_Email]   VARCHAR(100) NOT NULL PRIMARY KEY,
	[Category_Title]  VARCHAR(30) NOT NULL
	--CONSTRAINT FK_Category FOREIGN KEY (Category_Title) REFERENCES Category(Title) ON DELETE CASCADE ON UPDATE CASCADE
)
