CREATE TABLE [dbo].[Likes]
(
	[Client_Email]   VARCHAR(100) NOT NULL PRIMARY KEY,
	[Title_ID]  int NOT NULL
	CONSTRAINT FK_Category FOREIGN KEY (Title_ID) REFERENCES Category(Id) ON DELETE CASCADE ON UPDATE CASCADE
)
